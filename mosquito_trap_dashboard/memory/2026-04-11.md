## 2026-04-11
- Verified Mosquito Trap Dashboard V12 is running separately from older versions on port `7808` using `api_server_v12.py` and `dashboard_v12.html`.
- Confirmed local API `http://127.0.0.1:7808/api/v12/meta/info` returned version `12.0` during recheck.
- Public direct-IP access remained unreliable; operational preference is to use reverse-tunnel/public tunnel fallback instead of direct public IP path.
- Confirmed temporary public tunnel options tested on 2026-04-11 included localtunnel and Pinggy; tunnels are suitable as temporary access paths only.
- Implemented V12 frontend map change in `dashboard_v12.html` so the heatmap view now overlays numeric count badges on map points (via Leaflet `divIcon` markers / `heatCountLayer`) to satisfy the user requirement that the map show numbers like V11.
